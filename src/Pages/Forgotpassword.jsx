import React from 'react'

const Forgotpassword = () => {
  return (
    <div>
      {/* Role : {
        default : user;
        enum:["SA","A","D"]
      }

      const rolecheck = (role) => {
        try{
          const R = req.role
          if (Roles.include(R))
        }
      } */}
    </div>
  )
}

export default Forgotpassword
